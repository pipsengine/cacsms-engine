import type { MarketIntelligenceDashboard } from "../../lib/market-intelligence/types";
export function MarketIntelligenceStatusStrip({ data }: { data: MarketIntelligenceDashboard }) { return <section className="mi-status-strip">{[["Intelligence Status",data.status],["Proceed to Stage 1",data.proceedStatus],["Data Quality Score",`${data.dataQualityScore}%`],["Feed Freshness",data.feedFreshness],["Critical Sources",`${data.criticalSources.online} / ${data.criticalSources.total} Online`],["Optional Sources",`${data.optionalSources.online} / ${data.optionalSources.total} Available`]].map(([label,value])=><article key={label}><small>{label}</small><strong>{value}</strong></article>)}</section>; }
