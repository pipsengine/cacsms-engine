# MT5 Bridge

The MT5 bridge is the adapter boundary between CACSMS execution instructions and terminal-side Expert Advisors. It must authenticate each machine agent, use TLS 1.3 transport, enforce idempotent order correlation IDs, report acknowledgements, and stream terminal/account health.
